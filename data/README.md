# Data Directory

This directory stores:
- `recoverai.db` — SQLite database (auto-generated)
- `sample_failures.json` — Generated failure data
- `recovery_results.json` — Recovery outcomes
- Model files (`.joblib`) — Trained ML models

All files in this directory are gitignored except this README.
